userObject:
  name: SSHConnector
userInputs:
  BucketName: demo
  appPort: 22
  appURL: http://localhost.com
  userDefinedCredentials:
    SSH:
      UserName: "$USERNAME"
      SSHKey: "$SSH_PRIVATE_KEY"
