userObject:
  apps:
  - name: NoCredApp
    appURL: http://localhost.com
    appPort: "0"
    appTags:
      appType:
      - nocredapp
      environment:
      - logical
      execlevel:
      - app
    userDefinedCredentials:
      NoCred:
        Dummy: ""
  - name: HttpRequest
    appURL: http://localhost.com
    appPort: "0"
    appTags:
      appType:
      - httprequest
      environment:
      - logical
      execlevel:
      - app
      framework:
      - github
    userDefinedCredentials:
      JWTBearer:
        Algorithm: "RS256"
        PrivateKey: # GitHub application's private key file
        Payload: '{"iat": <<CURRENT_TIME>>, "exp": <<CURRENT_TIME + 600>>, "iss": [github-app-id]}'
        ValidationCURL: |
          curl --location --request POST 'https://api.github.com/app/installations/[github-app-installation-id]/access_tokens' \
            --header 'Authorization: Bearer <<JWTBearer>>>>' \
            --header 'Accept: application/vnd.github+json'
userInputs:
  CustomExceptionConfigFileForPR: <<MINIO_FILE_PATH>>
  InScopeRepos: <<MINIO_FILE_PATH>>
  PR-JQConfig: <<MINIO_FILE_PATH>>
  PRRequestConfig: <<MINIO_FILE_PATH>>
  PRResponseConfig: <<MINIO_FILE_PATH>>
  RequiredFields:
  - RepositoryName
  LogConfigFile: <<MINIO_FILE_PATH>>
  ProceedIfLogExists: true
  RuleOutputFileFormat: PARQUET
fromDate: 2025-02-13T09:12:12Z
toDate: 2025-02-13T09:12:12Z
