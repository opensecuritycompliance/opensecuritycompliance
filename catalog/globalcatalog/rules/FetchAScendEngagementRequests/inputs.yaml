userObject:
  apps:
  - name: HttpRequest
    appURL: http://localhost.com
    appPort: "0"
    appTags:
      appType:
      - httprequest
      environment:
      - logical
      execlevel:
      - app
    userDefinedCredentials:
      OAuth:
        ValidationCURL: |-
            curl --location 'https://auth.a-scend2.com/oauth/token' \
                --header 'Content-Type: application/x-www-form-urlencoded' \
                --data-urlencode 'grant_type=client_credentials' \
                --data-urlencode 'client_id=<<OAuth.ClientID>>' \
                --data-urlencode 'client_secret=<<OAuth.ClientSecret>>' \
                --data-urlencode 'audience=https://heimdall/'
        ClientID: ""
        ClientSecret: ""
  - name: NoCredApp
    appURL: http://localhost.com
    appPort: "0"
    appTags:
      appType:
      - nocredapp
      environment:
      - logical
      execlevel:
      - app
    userDefinedCredentials:
      NoCred:
        Dummy: ""
userInputs:
  EngagementsRequestConfig: <<MINIO_FILE_URL>>
  EngagementsResponseConfig: <<MINIO_FILE_URL>>
  InScopeEngagements: <<MINIO_FILE_URL>>
  RequestsSearchRequestConfig: <<MINIO_FILE_URL>>
  RequestsSearchResponseConfig: <<MINIO_FILE_URL>>
  RequestAssigneeMapping: <<MINIO_FILE_URL>>
  AssigneeMappingSQLConfig: <<MINIO_FILE_URL>>
  OutputFileFormat: JSON
  StandardizeRequestsJQConfig: <<MINIO_FILE_URL>>
fromDate: 2025-06-02T06:25:40Z
toDate: 2025-06-02T06:25:40Z
