userObject:
  app:
    name: NoCredApp
    appURL: http://localhost.com
    appPort: "0"
    appTags:
      appType:
      - nocredapp
      environment:
      - logical
      execlevel:
      - app
    userDefinedCredentials:
      NoCred:
        Dummy: ""
userInputs:
  InputFile1: file://OutputFile-d4daa6de-3ab1-4ef6-bcf1-4c8021dc9b6a.json
  InputFile2: file://sod_config.json
  SQLConfig: file://OpenLDAPSql.toml # SQL configuration file in TOML format. sample provided in SQLConfig-sample.toml
  SQLQuery: "" # SQL query to be executed on input files. If both SQLConfig and SQLQuery are provided, SQLConfig takes precedence.
  OutputFileFormat: "" # Output file format. Options are JSON, CSV or Parquet (Default: Parquet)
  LogConfigFile: <<MINIO_FILE_PATH>>
  LogFile: <<MINIO_FILE_PATH>>
  ProceedIfLogExists: # Proceed if log file exists. True or False (Default: True)
  ProceedIfErrorExists: # Proceed if error exists in current task. True or False (Default: True)
  ValidateFlow: # If True, only validates the input data and does not execute the SQL query. True or False (Default: False)
  OutputFileName: "" # Output filename without extension. Default is OutputFile
fromDate: 2025-04-23T09:11:52Z
toDate: 2025-04-23T09:11:52Z